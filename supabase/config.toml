[api]
enabled = true
port = 55421
schemas = ["public", "graphql_public", "board_pulse", "japanprchecker", "wheretayo", "thebudolfinds"]

# 54322 is reserved by Windows on some developer machines; keep the local database
# outside the reserved range; all local service ports are kept consistent below.
[db]
port = 55432

[studio]
port = 55423

[local_smtp]
port = 55424

[analytics]
port = 55427

[db.pooler]
port = 55429

[storage.vector]
enabled = false
